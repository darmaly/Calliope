---
phase: 08-mixer
plan: 04
type: execute
wave: 1
depends_on: ["08-03"]
files_modified:
  - app/src/renderer/components/mixer/EffectSlotList.tsx
  - app/src/renderer/components/mixer/EffectSlot.tsx
  - app/src/renderer/components/mixer/ChannelStrip.tsx
  - app/src/renderer/components/mixer/MasterStrip.tsx
  - app/src/renderer/App.tsx
  - app/src/renderer/stores/mixer-store.ts
  - app/src/renderer/types/mixer.ts
autonomous: true
gap_closure: true
requirements: [MIX-03]

must_haves:
  truths:
    - "Effect slots can be reordered by dragging within the effect slot list"
    - "Mixer panel height is resizable by dragging the split divider"
  artifacts:
    - path: "app/src/renderer/components/mixer/EffectSlotList.tsx"
      provides: "Drag-and-drop reorder for effect slots"
      contains: "onDragStart|onDragOver|onDrop|draggable"
    - path: "app/src/renderer/App.tsx"
      provides: "Functional mixer divider drag handler"
      contains: "mixerHeight.*Math"
  key_links:
    - from: "EffectSlotList.tsx"
      to: "mixer-store.ts"
      via: "onReorder callback → reorderTrackEffect / reorderMasterEffect"
      pattern: "onReorder"
    - from: "App.tsx"
      to: "mixer-store.ts or useState"
      via: "handleMixerDividerDrag updates mixerHeight"
      pattern: "mixerHeight.*deltaY"
---

<objective>
Close two verified gaps in the Phase 8 mixer implementation:

1. **Effect slot reorder** (MIX-03 partial): Wire drag-and-drop reorder in EffectSlotList so users can reorder effects within a track's insert chain. The store methods `reorderTrackEffect` and `reorderMasterEffect` already exist but are unreachable from the UI.

2. **Mixer panel resizable** (usability): Implement `handleMixerDividerDrag` in App.tsx so the mixer panel height is adjustable via the split divider, matching the piano roll panel pattern.

Purpose: Complete MIX-03 requirement and fix usability gap identified in 08-VERIFICATION.md.
Output: Updated EffectSlotList with drag-and-drop, functional mixer height resizing.
</objective>

<execution_context>
@/Users/deebarmaly/Calliope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deebarmaly/Calliope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mixer/08-VERIFICATION.md
@.planning/phases/08-mixer/08-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From app/src/renderer/types/mixer.ts:
```typescript
export interface EffectSlotInfo {
  effectType: string
  bypassed: boolean
}

export interface MixerActions {
  // ... existing actions
  reorderTrackEffect: (trackId: string, fromIndex: number, toIndex: number) => void
  reorderMasterEffect: (fromIndex: number, toIndex: number) => void
}
```

From app/src/renderer/components/mixer/EffectSlotList.tsx (current):
```typescript
interface EffectSlotListProps {
  slots: EffectSlotInfo[]
  onAdd: (effectType: string) => void
  onRemove: (index: number) => void
  onBypass: (index: number) => void
  onSlotClick: (index: number) => void
  // MISSING: onReorder prop
}
```

From app/src/renderer/components/piano-roll/SplitDivider.tsx:
```typescript
interface SplitDividerProps {
  onDrag: (deltaY: number) => void    // deltaY is pixels moved since last event
  onDoubleClick: () => void
}
```

From app/src/renderer/App.tsx (current problem):
```typescript
const mixerHeight = 300  // hardcoded, not stateful
const handleMixerDividerDrag = useCallback((deltaY: number) => {
  // empty no-op
}, [])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drag-and-drop reorder to EffectSlotList</name>
  <files>
    app/src/renderer/components/mixer/EffectSlotList.tsx
    app/src/renderer/components/mixer/EffectSlot.tsx
    app/src/renderer/components/mixer/ChannelStrip.tsx
    app/src/renderer/components/mixer/MasterStrip.tsx
  </files>
  <read_first>
    app/src/renderer/components/mixer/EffectSlotList.tsx
    app/src/renderer/components/mixer/EffectSlot.tsx
    app/src/renderer/components/mixer/ChannelStrip.tsx
    app/src/renderer/components/mixer/MasterStrip.tsx
    app/src/renderer/stores/mixer-store.ts
    app/src/renderer/types/mixer.ts
  </read_first>
  <action>
    Wire HTML5 drag-and-drop reorder for effect slots. This closes Gap 1 from VERIFICATION.md (MIX-03 partial).

    **EffectSlotList.tsx changes:**
    1. Add `onReorder: (fromIndex: number, toIndex: number) => void` to EffectSlotListProps interface.
    2. Track drag state with `useState<number | null>(null)` for the dragged index.
    3. Pass `draggable={true}`, `onDragStart`, `onDragOver`, `onDrop`, and `onDragEnd` to each filled EffectSlot.
    4. On drag start: set dragged index, set `e.dataTransfer.effectAllowed = 'move'`.
    5. On drag over: call `e.preventDefault()` to allow drop. Add a visual drop indicator (e.g., 2px top border `#6c63ff` on the target slot).
    6. On drop: call `onReorder(draggedIndex, targetIndex)` if indices differ.
    7. On drag end: clear dragged index state.
    8. Only filled slots (slot !== null) should be draggable. The empty "Add Effect" slot at the bottom is NOT draggable and NOT a drop target.

    **EffectSlot.tsx changes:**
    1. Add optional drag props to EffectSlotProps: `draggable?: boolean`, `onDragStart?: (e: React.DragEvent) => void`, `onDragOver?: (e: React.DragEvent) => void`, `onDrop?: (e: React.DragEvent) => void`, `onDragEnd?: () => void`, `isDragOver?: boolean`.
    2. Apply `draggable` attribute and event handlers to the outer div of filled slots.
    3. When `isDragOver` is true, show a 2px top border in `#6c63ff` to indicate drop position.
    4. Set `cursor: 'grab'` on filled slots (or add a grip icon via CSS).
    5. During drag (`isDragging`), reduce opacity to 0.4 on the dragged slot.

    **ChannelStrip.tsx changes:**
    1. Add a `handleReorderEffect` callback: `(fromIndex: number, toIndex: number) => { useMixerStore.getState().reorderTrackEffect(track.id, fromIndex, toIndex); try { window.calliope.effectReorder(track.id, fromIndex, toIndex) } catch {} }`.
    2. Pass `onReorder={handleReorderEffect}` to EffectSlotList.

    **MasterStrip.tsx changes:**
    1. Add a `handleReorderEffect` callback: `(fromIndex: number, toIndex: number) => { useMixerStore.getState().reorderMasterEffect(fromIndex, toIndex); try { window.calliope.effectReorder('master', fromIndex, toIndex) } catch {} }`.
    2. Pass `onReorder={handleReorderEffect}` to EffectSlotList.

    Use the same try/catch pattern around `window.calliope.*` calls that already exists in ChannelStrip and MasterStrip for other effect operations (engine may not be running).
  </action>
  <verify>
    <automated>cd /Users/deebarmaly/Calliope && npx tsc --noEmit --project app/tsconfig.json && grep -c "onReorder\|draggable\|onDragStart\|onDrop" app/src/renderer/components/mixer/EffectSlotList.tsx app/src/renderer/components/mixer/EffectSlot.tsx app/src/renderer/components/mixer/ChannelStrip.tsx app/src/renderer/components/mixer/MasterStrip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - EffectSlotList accepts `onReorder` prop of type `(fromIndex: number, toIndex: number) => void`
    - Filled EffectSlot elements have `draggable={true}` attribute
    - EffectSlot has onDragStart, onDragOver, onDrop, onDragEnd handlers
    - ChannelStrip passes `onReorder` to EffectSlotList calling `reorderTrackEffect`
    - MasterStrip passes `onReorder` to EffectSlotList calling `reorderMasterEffect`
    - TypeScript compiles with no errors
    - grep finds `onReorder` in all four files, `draggable` in EffectSlot.tsx and EffectSlotList.tsx
  </acceptance_criteria>
  <done>Effect slots are draggable and reorderable in both track and master strip effect lists. MIX-03 fully satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Implement mixer panel resize via split divider</name>
  <files>
    app/src/renderer/App.tsx
    app/src/renderer/stores/mixer-store.ts
    app/src/renderer/types/mixer.ts
  </files>
  <read_first>
    app/src/renderer/App.tsx
    app/src/renderer/stores/mixer-store.ts
    app/src/renderer/types/mixer.ts
  </read_first>
  <action>
    Implement mixer panel height resizing to close Gap 2 from VERIFICATION.md. Follow the exact same pattern as the piano roll panel height (usePianoRollStore.panelHeight + handleDividerDrag).

    **mixer-store.ts changes:**
    1. Add `mixerHeight: number` to MixerState (default `300`).
    2. Add `setMixerHeight: (height: number) => void` to MixerActions.
    3. Implement `setMixerHeight` in the store: `setMixerHeight: (height: number) => set({ mixerHeight: height })`.

    **types/mixer.ts changes:**
    1. Add `mixerHeight: number` to MixerState interface.
    2. Add `setMixerHeight: (height: number) => void` to MixerActions interface.

    **App.tsx changes:**
    1. Replace `const mixerHeight = 300` with `const mixerHeight = useMixerStore((s) => s.mixerHeight)`.
    2. Implement `handleMixerDividerDrag`:
       ```
       const handleMixerDividerDrag = useCallback((deltaY: number) => {
         const store = useMixerStore.getState()
         const maxH = window.innerHeight * 0.6
         const newH = Math.max(240, Math.min(maxH, store.mixerHeight - deltaY))
         store.setMixerHeight(newH)
       }, [])
       ```
       Note: min height is 240 (from UI spec), max is 60vh. The deltaY is subtracted because dragging up (negative deltaY) should increase height (panel grows upward).
    3. Keep `handleMixerDoubleClick` as-is (toggles visibility) — this is correct behavior.
  </action>
  <verify>
    <automated>cd /Users/deebarmaly/Calliope && npx tsc --noEmit --project app/tsconfig.json && grep -c "mixerHeight\|setMixerHeight" app/src/renderer/App.tsx app/src/renderer/stores/mixer-store.ts app/src/renderer/types/mixer.ts</automated>
  </verify>
  <acceptance_criteria>
    - `mixerHeight` field exists in MixerState with default 300
    - `setMixerHeight` action exists in MixerActions and is implemented in the store
    - App.tsx reads `mixerHeight` from `useMixerStore` (not hardcoded)
    - `handleMixerDividerDrag` computes new height using `Math.max(240, Math.min(maxH, store.mixerHeight - deltaY))`
    - TypeScript compiles with no errors
    - grep finds `mixerHeight` in all three files and `setMixerHeight` in store and types
  </acceptance_criteria>
  <done>Mixer panel is resizable via split divider with 240px minimum and 60vh maximum height. Height persists in Zustand store.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles cleanly: `npx tsc --noEmit --project app/tsconfig.json`
2. Existing mixer tests still pass: `npx vitest run test/mixer-store.test.ts test/mixer-helpers.test.ts`
3. Full test suite passes: `npx vitest run`
4. Effect reorder UI exists: `grep -l "draggable\|onDragStart\|onReorder" app/src/renderer/components/mixer/*.tsx` returns EffectSlot, EffectSlotList, ChannelStrip, MasterStrip
5. Mixer height is dynamic: `grep "useMixerStore.*mixerHeight\|store.mixerHeight" app/src/renderer/App.tsx` returns matches
6. No hardcoded `mixerHeight = 300` in App.tsx
</verification>

<success_criteria>
- MIX-03 fully satisfied: effect slots support add, remove, reorder (drag-and-drop), and bypass
- Mixer panel resizable via split divider (min 240px, max 60vh)
- All existing tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-mixer/08-04-SUMMARY.md`
</output>
