---
phase: 08-mixer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/renderer/types/mixer.ts
  - app/src/renderer/types/timeline.ts
  - app/src/renderer/stores/mixer-store.ts
  - app/src/renderer/utils/mixer-helpers.ts
  - app/src/renderer/components/mixer/MixerView.tsx
  - app/src/renderer/components/mixer/ChannelStrip.tsx
  - app/src/renderer/components/mixer/Fader.tsx
  - app/src/renderer/components/mixer/PanKnob.tsx
  - app/src/renderer/components/mixer/LevelMeter.tsx
  - app/src/renderer/components/mixer/EffectSlot.tsx
  - app/src/renderer/components/mixer/MasterStrip.tsx
  - app/src/renderer/App.tsx
  - test/mixer-store.test.ts
  - test/mixer-helpers.test.ts
autonomous: true
requirements:
  - MIX-01
  - MIX-02
  - MIX-03
  - MIX-04
---

<objective>
Build the complete mixer panel: types, Zustand store, helper utilities, and full UI with channel strips (volume fader, pan knob, mute/solo, effect inserts, level meters), master strip, and mixer toggle in the app layout.

Purpose: Delivers the mixer requirement (Phase 8 success criteria) in a single plan covering data layer, UI components, and app integration.
Output: Fully functional mixer panel with channel strips for all tracks plus master, toggleable from the app toolbar.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mixer/08-PENDING-DISCUSS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mixer types, store, helpers with tests</name>
  <files>
    app/src/renderer/types/mixer.ts,
    app/src/renderer/types/timeline.ts,
    app/src/renderer/stores/mixer-store.ts,
    app/src/renderer/utils/mixer-helpers.ts,
    test/mixer-store.test.ts,
    test/mixer-helpers.test.ts
  </files>
  <behavior>
    mixer-store tests:
    - "track volume": setTrackVolume('t1', 0.8) sets volume to 0.8; getTrackVolume returns 0.8; default is 1.0
    - "track pan": setTrackPan('t1', -0.5) sets pan to -0.5; range -1.0 to 1.0; default is 0.0
    - "track volume clamped": setTrackVolume('t1', 2.0) clamps to 1.259 (+6dB); setTrackVolume('t1', -1) clamps to 0
    - "master volume": setMasterVolume(0.7) sets masterVolume to 0.7; default is 1.0
    - "master pan": setMasterPan(0.3) sets masterPan to 0.3; default is 0.0
    - "level meters": setTrackLevel('t1', { peakL: 0.8, peakR: 0.6 }) stores levels; default is { peakL: 0, peakR: 0 }
    - "master level": setMasterLevel({ peakL: 0.9, peakR: 0.85 }) stores master levels
    - "effect slots": getTrackEffects('t1') returns empty array; add/remove/reorder/bypass tracked
    - "mixer visible": toggleMixerVisible flips mixerVisible from false to true

    mixer-helpers tests:
    - dbToLinear(0) === 1.0; dbToLinear(-6) approximately 0.501; dbToLinear(-Infinity) === 0
    - linearToDb(1.0) === 0; linearToDb(0.5) approximately -6.02; linearToDb(0) === -Infinity
    - formatDb(1.0) === '0.0 dB'; formatDb(0) === '-inf'; formatDb(0.5) === '-6.0 dB'
    - levelToMeterHeight(0, 120) === 0; levelToMeterHeight(1.0, 120) === 120; levelToMeterHeight(0.5, 120) close to half
    - panToString(-1) === '100L'; panToString(0) === 'C'; panToString(1) === '100R'; panToString(0.5) === '50R'
  </behavior>
  <action>
    1. Create mixer types
    2. Extend Track with volume/pan fields
    3. Create Zustand mixer store
    4. Create mixer helper utilities
    5. Write tests first (RED), then implement (GREEN)
  </action>
  <verify>
    <automated>cd /Users/deebarmaly/Calliope && npx vitest run test/mixer-store.test.ts test/mixer-helpers.test.ts</automated>
  </verify>
  <done>Mixer types exported, store functional with volume/pan/level/effects/visibility management, helpers pass all tests.</done>
</task>

<task type="auto">
  <name>Task 2: Mixer UI components and app integration</name>
  <files>
    app/src/renderer/components/mixer/MixerView.tsx,
    app/src/renderer/components/mixer/ChannelStrip.tsx,
    app/src/renderer/components/mixer/Fader.tsx,
    app/src/renderer/components/mixer/PanKnob.tsx,
    app/src/renderer/components/mixer/LevelMeter.tsx,
    app/src/renderer/components/mixer/EffectSlot.tsx,
    app/src/renderer/components/mixer/MasterStrip.tsx,
    app/src/renderer/App.tsx
  </files>
  <action>
    Build all mixer UI components and integrate into the app layout as a togglable bottom panel.
  </action>
  <verify>
    <automated>cd /Users/deebarmaly/Calliope && npx vitest run</automated>
  </verify>
  <done>Mixer panel renders with channel strips for each track plus master, faders, pan knobs, mute/solo buttons, effect slots, and level meters. Panel is toggleable from the app.</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
npx vitest run
```
All tests pass with no regressions.
</verification>

<success_criteria>
- Mixer store manages per-track volume, pan, and level meter data
- Each track has a channel strip with volume fader, pan knob, mute, and solo
- Master channel strip with volume fader and insert effect chain
- Per-track insert chain supports add, remove, reorder, and bypass per effect
- Level meters display signal level per channel strip
- Mixer toggleable from app layout
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-mixer/08-01-SUMMARY.md`
</output>
