---
phase: 10-application-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/renderer/stores/app-store.ts
  - app/src/renderer/utils/time-format.ts
  - app/src/renderer/components/transport/TransportBar.tsx
  - app/src/renderer/components/transport/TransportControls.tsx
  - app/src/renderer/components/transport/BpmDisplay.tsx
  - app/src/renderer/components/transport/TimeSignatureDisplay.tsx
  - app/src/renderer/components/transport/PositionDisplay.tsx
  - app/src/renderer/components/transport/LoopToggle.tsx
  - app/src/renderer/components/transport/MetronomeToggle.tsx
  - app/src/renderer/components/shared/SplitDivider.tsx
  - app/src/renderer/components/piano-roll/SplitDivider.tsx
  - test/time-format.test.ts
autonomous: true
requirements:
  - UI-02
  - UI-04

must_haves:
  truths:
    - "Transport bar displays above the timeline with play/stop/record buttons, BPM, time signature, and position"
    - "BPM value is editable via click-to-type input with Enter to confirm and Escape to cancel"
    - "Position display shows both MM:SS.ms and bars:beats format with tabular-nums to prevent jitter"
    - "Loop and metronome toggles show active/inactive state"
    - "Panel toggle buttons (Piano Roll, Mixer) appear in the transport bar right section"
    - "SplitDivider is available as a shared component"
  artifacts:
    - path: "app/src/renderer/stores/app-store.ts"
      provides: "Panel focus state management"
      exports: ["useAppStore"]
    - path: "app/src/renderer/utils/time-format.ts"
      provides: "Beat-to-time and beat-to-bars conversion"
      exports: ["beatToTimeMs", "formatTimeMs", "beatToBarsBeats", "formatBarsBeats"]
    - path: "app/src/renderer/components/transport/TransportBar.tsx"
      provides: "Top-level transport bar container"
      exports: ["TransportBar"]
    - path: "test/time-format.test.ts"
      provides: "Unit tests for time formatting"
  key_links:
    - from: "app/src/renderer/components/transport/TransportBar.tsx"
      to: "app/src/renderer/stores/timeline-store.ts"
      via: "Zustand selectors for bpm, timeSignature, currentBeat, isPlaying"
      pattern: "useTimelineStore"
    - from: "app/src/renderer/components/transport/TransportControls.tsx"
      to: "window.calliope"
      via: "IPC bridge calls"
      pattern: "window\\.calliope\\.transport"
---

<objective>
Build the transport bar, app-store, time-format utilities, and move SplitDivider to shared.

Purpose: Create the always-visible transport bar that provides temporal context (play state, position, tempo) and unified panel navigation. This is the primary new UI component of Phase 10.
Output: TransportBar component with all sub-components, app-store for panel focus, time-format utilities with tests, SplitDivider relocated to shared.
</objective>

<execution_context>
@/Users/deebarmaly/Calliope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deebarmaly/Calliope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-application-integration/10-CONTEXT.md
@.planning/phases/10-application-integration/10-RESEARCH.md
@.planning/phases/10-application-integration/10-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/src/renderer/stores/timeline-store.ts:
```typescript
export const useTimelineStore = create<TimelineState & TimelineActions>((set, get) => ({
  isPlaying: false,
  currentBeat: 0,
  bpm: 120,
  timeSignature: { numerator: 4, denominator: 4 },
  // ... setTransportState writes these from usePlayhead hook
}))
```

From app/src/renderer/types/calliope.d.ts:
```typescript
interface TransportState {
  state: 'stopped' | 'playing' | 'paused'
  bpm: number
  timeSigNumerator: number
  timeSigDenominator: number
  samplePosition: number
  ppqPosition: number       // beat position
  looping: boolean
  loopStartBeat: number
  loopEndBeat: number
}
```

IPC methods available on window.calliope:
```typescript
transportPlay(): Promise<void>
transportStop(): Promise<void>
transportPause(): Promise<void>
setBpm(bpm: number): Promise<void>
setTimeSignature(num: number, den: number): Promise<void>
setLoopRegion(start: number, end: number, enabled: boolean): Promise<void>
setMetronomeEnabled(enabled: boolean): Promise<void>
commandUndo(): Promise<void>
commandRedo(): Promise<void>
projectSave(): Promise<{ success: boolean; filePath?: string }>
```

From app/src/renderer/components/piano-roll/SplitDivider.tsx:
```typescript
interface SplitDividerProps {
  onDrag: (deltaY: number) => void
  onDoubleClick: () => void
}
export function SplitDivider({ onDrag, onDoubleClick }: SplitDividerProps)
```

From app/src/renderer/stores/piano-roll-store.ts:
```typescript
activeClipId: string | null
setActiveClip: (clipId: string | null) => void
```

From app/src/renderer/stores/mixer-store.ts:
```typescript
mixerVisible: boolean
toggleMixerVisible: () => void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app-store, time-format utilities with tests, and move SplitDivider to shared</name>
  <files>
    app/src/renderer/stores/app-store.ts,
    app/src/renderer/utils/time-format.ts,
    test/time-format.test.ts,
    app/src/renderer/components/shared/SplitDivider.tsx,
    app/src/renderer/components/piano-roll/SplitDivider.tsx,
    app/src/renderer/components/piano-roll/PianoRollPanel.tsx,
    app/src/renderer/App.tsx
  </files>
  <read_first>
    app/src/renderer/stores/timeline-store.ts,
    app/src/renderer/components/piano-roll/SplitDivider.tsx,
    app/src/renderer/components/piano-roll/PianoRollPanel.tsx,
    app/src/renderer/App.tsx,
    test/waveform-utils.test.ts
  </read_first>
  <behavior>
    - beatToTimeMs(0, 120) => 0
    - beatToTimeMs(120, 120) => 60000
    - beatToTimeMs(1, 60) => 1000
    - formatTimeMs(0) => "00:00.000"
    - formatTimeMs(61500) => "01:01.500"
    - formatTimeMs(3723456) => "62:03.456"
    - beatToBarsBeats(0, 4) => { bar: 1, beatInBar: 1 }
    - beatToBarsBeats(4, 4) => { bar: 2, beatInBar: 1 }
    - beatToBarsBeats(7, 4) => { bar: 2, beatInBar: 4 }
    - beatToBarsBeats(0, 3) => { bar: 1, beatInBar: 1 } (3/4 time)
    - formatBarsBeats(1, 1) => "001:01"
    - formatBarsBeats(12, 3) => "012:03"
  </behavior>
  <action>
    1. Create `app/src/renderer/stores/app-store.ts`:
       - Zustand store with `focusedPanel: 'timeline' | 'piano-roll' | 'mixer'` defaulting to `'timeline'`
       - Action `setFocusedPanel: (panel) => void`
       - Follow existing create<> pattern from timeline-store

    2. Create `app/src/renderer/utils/time-format.ts`:
       - `beatToTimeMs(beat: number, bpm: number): number` -- (beat / bpm) * 60000
       - `formatTimeMs(ms: number): string` -- returns "MM:SS.ms" zero-padded (minutes can exceed 59)
       - `beatToBarsBeats(beat: number, beatsPerBar: number): { bar: number; beatInBar: number }` -- 1-indexed bars and beats
       - `formatBarsBeats(bar: number, beat: number): string` -- returns "NNN:NN" zero-padded

    3. Create `test/time-format.test.ts` with tests per behavior block above. Write tests FIRST, verify they fail, then implement.

    4. Move SplitDivider:
       - Copy `app/src/renderer/components/piano-roll/SplitDivider.tsx` to `app/src/renderer/components/shared/SplitDivider.tsx` (identical content)
       - Update import in `App.tsx`: change from `'./components/piano-roll/SplitDivider'` to `'./components/shared/SplitDivider'`
       - Update import in `PianoRollPanel.tsx` if it imports SplitDivider (check first)
       - Keep the old file as a re-export: `export { SplitDivider } from '../shared/SplitDivider'` so any other existing imports still work
       - Grep the codebase for `'../piano-roll/SplitDivider'` and `'./piano-roll/SplitDivider'` to find all consumers
  </action>
  <verify>
    <automated>cd /Users/deebarmaly/Calliope && pnpm vitest run test/time-format.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - app/src/renderer/stores/app-store.ts exports useAppStore with focusedPanel field
    - app/src/renderer/utils/time-format.ts exports beatToTimeMs, formatTimeMs, beatToBarsBeats, formatBarsBeats
    - test/time-format.test.ts passes with all behavior cases
    - app/src/renderer/components/shared/SplitDivider.tsx exists with identical functionality
    - App.tsx imports SplitDivider from shared path
    - No build errors from import path changes
  </acceptance_criteria>
  <done>App-store created, time-format utilities tested and passing, SplitDivider relocated to shared with all imports updated</done>
</task>

<task type="auto">
  <name>Task 2: Build TransportBar with all sub-components</name>
  <files>
    app/src/renderer/components/transport/TransportBar.tsx,
    app/src/renderer/components/transport/TransportControls.tsx,
    app/src/renderer/components/transport/BpmDisplay.tsx,
    app/src/renderer/components/transport/TimeSignatureDisplay.tsx,
    app/src/renderer/components/transport/PositionDisplay.tsx,
    app/src/renderer/components/transport/LoopToggle.tsx,
    app/src/renderer/components/transport/MetronomeToggle.tsx,
    app/src/renderer/components/timeline/TimelineToolbar.tsx,
    app/src/renderer/App.tsx
  </files>
  <read_first>
    app/src/renderer/App.tsx,
    app/src/renderer/components/timeline/TimelineToolbar.tsx,
    app/src/renderer/stores/timeline-store.ts,
    app/src/renderer/stores/app-store.ts,
    app/src/renderer/utils/time-format.ts,
    app/src/renderer/hooks/use-playhead.ts,
    app/src/renderer/stores/piano-roll-store.ts,
    app/src/renderer/stores/mixer-store.ts,
    .planning/phases/10-application-integration/10-UI-SPEC.md
  </read_first>
  <action>
    Build the TransportBar per the 10-UI-SPEC.md layout specification. All sub-components are small stateless/semi-stateless components.

    1. **TransportControls.tsx**: Play/Stop/Record button group.
       - Play button: lucide `Play` icon when stopped, `Pause` icon when playing. 32x32px, rounded-md. Stopped: bg-[#3a3a5a] text-[#eeeeee]. Playing: bg-[#6c63ff] text-white. Calls `window.calliope.transportPlay()` or `window.calliope.transportStop()`. Subscribe to `isPlaying` from useTimelineStore.
       - Stop button: lucide `Square` icon. Always bg-[#3a3a5a] text-[#eeeeee], hover bg-[#4a4a6a]. Calls `window.calliope.transportStop()`.
       - Record button: CSS circle (w-3 h-3 rounded-full). Inactive: bg-[#3a3a5a] circle-[#999999]. Active: bg-[#ef4444] circle-white, animate-pulse. Track local `isArmed` state (placeholder, no engine call yet). Tooltip per 10-UI-SPEC.md copywriting contract.
       - Gap between buttons: 4px (gap-1).

    2. **BpmDisplay.tsx**: Editable BPM readout.
       - Display mode: 16px semibold #eeeeee for numeric value, 10px #999999 "BPM" label below. Width 72px.
       - Click triggers edit mode: renders `<input type="number" min={20} max={300} step={1}>` with bg-[#1a1a2e] text-[#eeeeee] border-[#6c63ff].
       - CRITICAL pitfall: Track `isEditing` local state. When isEditing=true, use local `editValue` state, do NOT subscribe to store bpm (prevents rAF overwrite). Commit on Enter/blur via `window.calliope.setBpm(value)`. Cancel on Escape.
       - Apply `tabular-nums` to prevent digit width jitter.

    3. **TimeSignatureDisplay.tsx**: Read-only time signature.
       - 16px semibold #eeeeee for numerator/denominator with "/" separator. Width 48px.
       - Subscribe to `timeSignature` from useTimelineStore.

    4. **PositionDisplay.tsx**: Dual-format position.
       - Time: MM:SS.ms using `formatTimeMs(beatToTimeMs(currentBeat, bpm))`. Width 120px.
       - Bars:Beats: using `beatToBarsBeats(currentBeat, timeSignature.numerator)` then `formatBarsBeats(bar, beatInBar)`. Width 96px.
       - Labels: 10px #999999 "TIME" and "POSITION" below each readout.
       - Vertical 1px #3a3a5a divider between them.
       - Subscribe ONLY to `currentBeat`, `bpm`, `timeSignature` from useTimelineStore via useShallow. This component updates at ~30Hz.
       - Apply `tabular-nums` and `font-semibold text-[16px]` to numeric displays.

    5. **LoopToggle.tsx**: lucide `Repeat` icon, 32x32 toggle button.
       - Inactive: bg-[#3a3a5a] text-[#999999]. Active: bg-[#6c63ff] text-white.
       - Track local `loopEnabled` state, toggle via `window.calliope.setLoopRegion?.(0, 0, !loopEnabled)`. Or read from `useTimelineStore` if loopRegion is set.
       - Tooltip: "Loop (L)".

    6. **MetronomeToggle.tsx**: lucide `Timer` icon, 32x32 toggle button.
       - Same pattern as LoopToggle. Calls `window.calliope.setMetronomeEnabled(!enabled)`.
       - Track local `metronomeEnabled` state. Tooltip: "Metronome".

    7. **TransportBar.tsx**: Container that arranges all sections per 10-UI-SPEC.md layout table.
       - Height 48px, bg-[#252542], border-b border-[#3a3a5a], px-4, flex items-center.
       - Left to right: TransportControls | separator | BpmDisplay + TimeSignatureDisplay | separator | PositionDisplay | separator | LoopToggle + MetronomeToggle | flex-1 spacer | Panel toggles (right).
       - Separators: 1px wide div, h-5, bg-[#3a3a5a], mx-4.
       - Panel toggles: Move Piano Roll and Mixer toggle buttons from TimelineToolbar into TransportBar right section. Use same logic/styling from TimelineToolbar but inside TransportBar.

    8. **Update TimelineToolbar.tsx**: Remove Piano Roll and Mixer toggle buttons. Keep only GridResolutionSelect, Snap toggle, and Zoom controls.

    9. **Update App.tsx**: Add `<TransportBar />` as the first child inside the flex-col container, above the timeline. Import from `'./components/transport/TransportBar'`.

    All buttons use pointer events. All tooltips use `title` attribute. No `onClick` on the transport bar itself.
  </action>
  <verify>
    <automated>cd /Users/deebarmaly/Calliope && pnpm exec tsc --noEmit --project app/tsconfig.renderer.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - app/src/renderer/components/transport/TransportBar.tsx exists and exports TransportBar
    - app/src/renderer/components/transport/TransportControls.tsx exports TransportControls with Play/Stop/Record
    - app/src/renderer/components/transport/BpmDisplay.tsx exports BpmDisplay with click-to-edit
    - app/src/renderer/components/transport/PositionDisplay.tsx exports PositionDisplay with dual format
    - app/src/renderer/components/transport/TimeSignatureDisplay.tsx exports TimeSignatureDisplay
    - app/src/renderer/components/transport/LoopToggle.tsx exports LoopToggle
    - app/src/renderer/components/transport/MetronomeToggle.tsx exports MetronomeToggle
    - App.tsx renders TransportBar above TimelineView
    - TimelineToolbar.tsx no longer contains Piano Roll or Mixer toggle buttons
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>TransportBar with all sub-components renders in the app, panel toggles moved from TimelineToolbar, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `pnpm vitest run test/time-format.test.ts -x` -- time format utilities pass
2. `pnpm exec tsc --noEmit --project app/tsconfig.renderer.json` -- no type errors
3. `pnpm run dev` launches and shows transport bar above timeline
</verification>

<success_criteria>
- Transport bar visible at top of app with play/stop/record, BPM, time signature, position, loop, metronome
- BPM is editable via click-to-type
- Position displays update during playback (MM:SS.ms and bars:beats)
- Panel toggle buttons (Piano Roll, Mixer) are in transport bar, removed from TimelineToolbar
- SplitDivider available from shared path
- Time format tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-application-integration/10-01-SUMMARY.md`
</output>
