---
phase: 10-application-integration
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/src/renderer/hooks/use-keyboard-shortcuts.ts
  - app/src/renderer/hooks/use-piano-roll-shortcuts.ts
  - app/src/renderer/App.tsx
  - app/src/renderer/components/timeline/TimelineView.tsx
  - app/src/renderer/components/piano-roll/PianoRollPanel.tsx
  - app/src/renderer/components/mixer/MixerPanel.tsx
  - app/src/renderer/components/timeline/AudioClip.tsx
  - test/keyboard-shortcuts.test.ts
autonomous: false
requirements:
  - UI-02
  - UI-03
  - UI-05

must_haves:
  truths:
    - "Keyboard shortcuts for transport (Space, R, L), undo/redo (Ctrl+Z/Shift+Z), and save (Ctrl+S) work globally"
    - "Panel-specific shortcuts (Delete, Ctrl+A, Ctrl+D, Escape, Q) route to the focused panel only"
    - "Clicking a panel visually focuses it with a 2px left accent border"
    - "Audio clip waveforms render via PixiJS with peak data or fallback dashed line"
    - "Window title shows project name and dirty indicator"
    - "All panels visible in a unified cohesive layout"
  artifacts:
    - path: "app/src/renderer/hooks/use-keyboard-shortcuts.ts"
      provides: "Focus-aware unified keyboard shortcut handler"
      exports: ["useKeyboardShortcuts"]
    - path: "test/keyboard-shortcuts.test.ts"
      provides: "Unit tests for shortcut routing logic"
  key_links:
    - from: "app/src/renderer/hooks/use-keyboard-shortcuts.ts"
      to: "app/src/renderer/stores/app-store.ts"
      via: "Reads focusedPanel to route shortcuts"
      pattern: "useAppStore\\.getState\\(\\)\\.focusedPanel"
    - from: "app/src/renderer/App.tsx"
      to: "app/src/renderer/stores/app-store.ts"
      via: "Panel containers set focus on pointerdown"
      pattern: "setFocusedPanel"
    - from: "app/src/renderer/App.tsx"
      to: "app/src/renderer/stores/project-store.ts"
      via: "useEffect updates document.title from projectName + isDirty"
      pattern: "document\\.title"
---

<objective>
Wire panel focus system, refactor keyboard shortcuts to be focus-aware, verify waveform rendering, add window title, and human-verify the complete integration.

Purpose: Complete the application integration by making keyboard shortcuts context-aware, adding visual focus indicators, ensuring waveform rendering works, and polishing the unified layout.
Output: Focus-aware shortcut system, panel focus UI, window title, verified waveform rendering, fully integrated DAW workspace.
</objective>

<execution_context>
@/Users/deebarmaly/Calliope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/deebarmaly/Calliope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-application-integration/10-CONTEXT.md
@.planning/phases/10-application-integration/10-RESEARCH.md
@.planning/phases/10-application-integration/10-UI-SPEC.md
@.planning/phases/10-application-integration/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From app/src/renderer/stores/app-store.ts (created in Plan 01):
```typescript
import { create } from 'zustand'

type FocusablePanel = 'timeline' | 'piano-roll' | 'mixer'

interface AppState {
  focusedPanel: FocusablePanel
  setFocusedPanel: (panel: FocusablePanel) => void
}

export const useAppStore = create<AppState>(...)
```

From app/src/renderer/utils/time-format.ts (created in Plan 01):
```typescript
export function beatToTimeMs(beat: number, bpm: number): number
export function formatTimeMs(ms: number): string
export function beatToBarsBeats(beat: number, beatsPerBar: number): { bar: number; beatInBar: number }
export function formatBarsBeats(bar: number, beat: number): string
```

From existing hooks:
```typescript
// use-keyboard-shortcuts.ts -- currently handles timeline shortcuts + global (Space, Ctrl+Z, etc.)
// use-piano-roll-shortcuts.ts -- capture-phase listener, active when activeClipId !== null
// Both will be refactored into a single focus-aware system
```

From app/src/renderer/stores/project-store.ts:
```typescript
projectName: string     // "Untitled" default
isDirty: boolean        // unsaved changes flag
filePath: string | null // current file path
save: () => Promise<boolean>
```

From app/src/renderer/components/timeline/AudioClip.tsx:
```typescript
// Already renders waveforms via PixiJS poly() with peaksToVertices()
// Uses clip.peakData (Float32Array), falls back to dashed center line
// cullable={true} already set
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor keyboard shortcuts into focus-aware system with panel focus wiring</name>
  <files>
    app/src/renderer/hooks/use-keyboard-shortcuts.ts,
    app/src/renderer/hooks/use-piano-roll-shortcuts.ts,
    app/src/renderer/App.tsx,
    app/src/renderer/components/timeline/TimelineView.tsx,
    app/src/renderer/components/piano-roll/PianoRollPanel.tsx,
    app/src/renderer/components/mixer/MixerPanel.tsx,
    test/keyboard-shortcuts.test.ts
  </files>
  <read_first>
    app/src/renderer/hooks/use-keyboard-shortcuts.ts,
    app/src/renderer/hooks/use-piano-roll-shortcuts.ts,
    app/src/renderer/App.tsx,
    app/src/renderer/stores/app-store.ts,
    app/src/renderer/stores/timeline-store.ts,
    app/src/renderer/stores/piano-roll-store.ts,
    app/src/renderer/stores/mixer-store.ts,
    app/src/renderer/stores/project-store.ts,
    app/src/renderer/components/timeline/TimelineView.tsx,
    app/src/renderer/components/piano-roll/PianoRollPanel.tsx,
    app/src/renderer/components/mixer/MixerPanel.tsx,
    .planning/phases/10-application-integration/10-UI-SPEC.md
  </read_first>
  <behavior>
    Test the shortcut routing LOGIC as pure functions (extract routing logic into a testable function):
    - routeShortcut('Space', no_mod, 'timeline') => 'global:play-toggle'
    - routeShortcut('Space', no_mod, 'piano-roll') => 'global:play-toggle'
    - routeShortcut('Delete', no_mod, 'timeline') => 'timeline:delete-selected'
    - routeShortcut('Delete', no_mod, 'piano-roll') => 'piano-roll:delete-selected'
    - routeShortcut('Delete', no_mod, 'mixer') => null (no action)
    - routeShortcut('z', ctrl, 'timeline') => 'global:undo'
    - routeShortcut('z', ctrl, 'piano-roll') => 'global:undo'
    - routeShortcut('s', ctrl, any_panel) => 'global:save'
    - routeShortcut('r', no_mod, any_panel) => 'global:record-toggle'
    - routeShortcut('l', no_mod, any_panel) => 'global:loop-toggle'
    - routeShortcut('q', no_mod, 'piano-roll') => 'piano-roll:quantize'
    - routeShortcut('q', no_mod, 'timeline') => null (no action)
    - routeShortcut('a', ctrl, 'timeline') => 'timeline:select-all'
    - routeShortcut('a', ctrl, 'piano-roll') => 'piano-roll:select-all'
    - routeShortcut('e', ctrl+shift, any_panel) => 'global:export'
    - routeShortcut('s', no_mod, 'timeline') => 'timeline:toggle-snap'
    - routeShortcut('m', no_mod, 'mixer') => 'mixer:toggle-mute'
    - Input target (INPUT/TEXTAREA) => null for all shortcuts
  </behavior>
  <action>
    1. Create `test/keyboard-shortcuts.test.ts` FIRST with tests for pure routing logic per behavior block. Run tests -- they must fail (RED).

    2. Refactor `use-keyboard-shortcuts.ts` into a focus-aware unified handler:
       - Extract a pure function `routeShortcut(key: string, modifiers: { ctrl: boolean, shift: boolean, alt: boolean }, focusedPanel: FocusablePanel): string | null` that returns an action string or null.
       - Export this function for testing.
       - The hook now:
         a. Reads `focusedPanel` from `useAppStore.getState()`
         b. Calls `routeShortcut()` to determine action
         c. Dispatches the action to the appropriate handler
       - Global shortcuts (always fire regardless of panel): Space (play/stop), R (record toggle), L (loop toggle), Ctrl+Z (undo), Ctrl+Shift+Z (redo), Ctrl+S (save via `useProjectStore.getState().save()`), Ctrl+Shift+E (export dialog via `useProjectStore.getState().showExportDialog()`)
       - Timeline shortcuts (only when focusedPanel='timeline'): Delete (delete clips), Ctrl+A (select all clips), Ctrl+D (duplicate clips), Escape (clear selection), S (toggle snap), Ctrl+=/- (zoom)
       - Piano roll shortcuts (only when focusedPanel='piano-roll'): Delete (delete notes), Ctrl+A (select all notes), Ctrl+D (duplicate notes), Ctrl+C/V/X (copy/paste/cut), Escape (deselect or close), Q (quantize)
       - Mixer shortcuts (only when focusedPanel='mixer'): M (toggle mute), S (toggle solo)
       - Guard: skip all shortcuts when `e.target.tagName === 'INPUT' || 'TEXTAREA' || isContentEditable`
       - Register as capture-phase listener (`{ capture: true }`) so it fires before any bubble-phase listeners
       - Use `e.preventDefault()` and `e.stopImmediatePropagation()` for handled shortcuts

    3. Remove `use-piano-roll-shortcuts.ts` hook usage. Delete the file or convert it to a re-export stub that does nothing (since all piano roll shortcuts are now in the unified handler). The existing `usePianoRollShortcuts()` call in PianoRollPanel.tsx should be removed.

    4. Wire panel focus in App.tsx:
       - Import `useAppStore` from stores/app-store
       - Wrap each panel section (TimelineView container, PianoRollPanel container, MixerPanel container) with an `onPointerDown` handler that calls `useAppStore.getState().setFocusedPanel('timeline' | 'piano-roll' | 'mixer')`
       - Apply focus border: read `focusedPanel` from useAppStore, apply `border-l-2 border-[#6c63ff]` to focused panel container, `border-l-2 border-transparent` to unfocused
       - Add window title effect: `useEffect` subscribing to `projectName` and `isDirty` from useProjectStore, updating `document.title` to `${isDirty ? '* ' : ''}${projectName} - LuneyTunes`

    5. Run tests (GREEN). Ensure all routing tests pass.
  </action>
  <verify>
    <automated>cd /Users/deebarmaly/Calliope && pnpm vitest run test/keyboard-shortcuts.test.ts -x && pnpm exec tsc --noEmit --project app/tsconfig.renderer.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - test/keyboard-shortcuts.test.ts passes with all routing behavior tests
    - use-keyboard-shortcuts.ts exports routeShortcut function and useKeyboardShortcuts hook
    - use-piano-roll-shortcuts.ts is no longer called (removed from PianoRollPanel or deleted)
    - App.tsx has onPointerDown handlers on each panel container that set focusedPanel
    - Focused panel has border-l-2 border-[#6c63ff], unfocused has border-transparent
    - document.title updates reactively from projectName and isDirty
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>Keyboard shortcuts route correctly by panel focus, visual focus indicators work, window title reflects project state</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete application integration</name>
  <what-built>
    Complete Phase 10 application integration:
    - Transport bar with play/stop/record, BPM (editable), time signature, position display, loop/metronome toggles, panel toggles
    - Focus-aware keyboard shortcuts (global + panel-specific)
    - Panel focus indicators (accent left border)
    - Audio clip waveform rendering via PixiJS (with fallback for no peak data)
    - Window title with project name and dirty indicator
    - Unified layout: TransportBar > Timeline > PianoRoll > Mixer
  </what-built>
  <how-to-verify>
    Start the app: `cd /Users/deebarmaly/Calliope && pnpm run dev`

    1. **Transport bar visible**: Confirm the transport bar appears at the top with play/stop/record buttons, BPM display, time signature, position readouts, loop toggle, metronome toggle, and Piano Roll / Mixer toggles on the right.

    2. **Transport controls work**:
       - Click Play -- button turns accent color, position displays update
       - Click Stop -- transport resets
       - Click BPM value -- editable input appears. Type a new BPM, press Enter. Confirm it commits. Press Escape on a second edit to cancel.

    3. **Panel toggles**: Click Piano Roll toggle in transport bar -- piano roll panel opens. Click Mixer toggle -- mixer panel opens. Both panels visible simultaneously.

    4. **Panel focus**: Click on the timeline area -- it gets a subtle accent left border. Click on piano roll -- focus border moves there. Click on mixer -- focus moves there.

    5. **Keyboard shortcuts**:
       - Press Space -- toggles play/stop (global, works from any panel)
       - Press Ctrl+Z -- undo (global)
       - Press Ctrl+S -- save (global)
       - Focus timeline, press Delete with clips selected -- deletes clips
       - Focus piano roll, press Delete with notes selected -- deletes notes (not clips)
       - Focus piano roll, press Q -- quantizes notes
       - Type in BPM input -- shortcuts do NOT fire while typing

    6. **Waveform rendering**: If audio clips exist on the timeline, confirm they render waveforms (or dashed fallback line) via PixiJS.

    7. **Window title**: Check the window title bar shows "Untitled - LuneyTunes" (or "* Untitled - LuneyTunes" if dirty).

    8. **Resize dividers**: Drag the divider between timeline and piano roll -- resizing works. Drag between piano roll and mixer -- works.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm vitest run test/keyboard-shortcuts.test.ts test/time-format.test.ts -x` -- all tests pass
2. `pnpm exec tsc --noEmit --project app/tsconfig.renderer.json` -- no type errors
3. `pnpm run dev` -- app launches with transport bar, shortcuts work, panels focus correctly
4. Human verification of visual integration complete
</verification>

<success_criteria>
- All keyboard shortcuts route correctly by panel focus (global always fires, panel-specific only when focused)
- Visual focus indicator (accent left border) shows on the active panel
- Window title shows project name with dirty indicator
- Audio clip waveforms render via PixiJS/WebGL
- Complete unified DAW layout: transport bar > timeline > piano roll > mixer
- Human has verified the full integration
</success_criteria>

<output>
After completion, create `.planning/phases/10-application-integration/10-02-SUMMARY.md`
</output>
